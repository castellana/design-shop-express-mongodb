const toggleHide = () => {
    // cuando hacemos click, se abre un nuevo formulario
    // document.getElementById('product').classList.toggle('hide')
    document.getElementById('myForm').classList.toggle('hide')
}

// const test = () => { 
//     let index = Math.floor(Math.random() * productData.length)
//     console.log('Index:' , index);
      //así se elije siempre un elemento random de un array! Y da igual si se eliminan o anaden-->
    // let element = productData[index].productName //así seleccionados sólo 1 elemento del array y sólo a 1 Key del objeto 
    //  arr.slice(0, 5)
// } 






    